//1. Create server.js file
// Initialize npm and install dependencies
// express, dotenv, cors, mongodb, ejs, nodemon, (--save-dev) insert "dev": "nodemon server.js" in package.json file in scripts. run with: npm run dev

//2. Require dependencies in server.js

//3. Declare variables
     Create gitignore file and push to github

//4. Connect to mongoDB - add connection string to .env file add env to gitignore flie

//5. create port

//------Test Mongo and port connection

//6. Set middleware

//7.

//8. Create public view folders - add main.js and style.css to public and index.ejs to views

//9. add content to main.js, style.css, index.ejs

//10. Create heroku repo 


// heroku login
// heroku create simple-rap-api
// echo "web: node server.js" > procfile
// git add .
// git commit -m "changes"
// git push heroku main
//-----------Test Heroku Link

npm init ; npm install express ;npm install dotenv ; npm install cors; npm install mongodb; npm install ejs;npm install nodemon --save-dev


run server with nodeserver.js (have to costantly stop and start)
run nodemon (dynamycly/automaticaly )
hello